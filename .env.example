# Optional auto-send after approval. The studio stays usable without these.

# Local MiniMax H3 bot on the 4080 (preferred). POST JSON { source, clips, text }.
# TELEGRAM_WEBHOOK_URL=http://127.0.0.1:8787/generate

# Or send the six messages into a Telegram chat (free Bot API, not MiniMax cloud).
# TELEGRAM_BOT_TOKEN=
# CHAT_ID=
# TELEGRAM_CHAT_ID=

# Public UI hints. Bot username is unknown until pasted.
# NEXT_PUBLIC_TELEGRAM_BOT=
# NEXT_PUBLIC_TELEGRAM_PREFIX=
